//- Word card
.word-card
  .card.hoverable

    //- Word image
    .card-image.waves-effect.waves-block.waves-light
      img.activator(src="{{image}}")

    .card-content
      //- Timer
      .progress
        .determinate(style="width: {{time}}%")

    //- Word hint
    .card-reveal.white-text
      span.card-title Hint
        i.material-icons.right close
      p {{hint}}

    //- Controls
    .card-action
      //- Volume on/off
      button.btn-floating.waves-effect.waves-dark((click)="toggleVolume()")
        i(*ngIf="isVolumeEnabled").material-icons volume_up
        i(*ngIf="!isVolumeEnabled").material-icons volume_off
      
      //- Current word index from word list
      span.btn-floating.btn-large.waves-effect.waves-dark
        i {{currentWordIndex}}/10

      //- Replay sound
      button.btn-floating.waves-effect.waves-dark((click)="repeat()")
        i.material-icons repeat
